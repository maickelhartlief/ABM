###### skeleton.py
# A configuration file with hyperparameters. Currently just uses all parameters from 
# normal.py, but can be copied and renamed and paramters can be overwritten by adding 
# them here under "parameters to overwrite". The config can then be used by using the 
# name of the file (excluding .py) as the input variable when running run.py or 
# sobol.py or ofat.py
####

# Use parameters in normal.py as default
from configs.normal import *

# The paramters to overwrite